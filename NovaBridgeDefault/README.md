# NovaBridgeDefault

Minimal UE project shell that can be used for headless startup.

Example:

```bash
UnrealEditor /path/to/NovaBridgeDefault/NovaBridgeDefault.uproject -RenderOffScreen -nosplash -nosound -unattended -nopause
```
